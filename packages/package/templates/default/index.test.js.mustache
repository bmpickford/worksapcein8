import { expect } from 'chai';
import { my_package_name } from './index.js';

describe('{{package_name}}', () => {
    it('should return the correct package name', () => {
        expect(my_package_name).to.equal('{{package_name}}');
    });
});